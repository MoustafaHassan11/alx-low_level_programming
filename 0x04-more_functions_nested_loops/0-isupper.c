#include "main.h"

/**
 * _isupper - checks if a character is an uppercase letter.
 *
 * @charcter: the character to be checked.
 *
 * Return: 1 if the character is an uppercase letter, 0 otherwise.
 */

int _isupper(int charcter)
{
	if (charcter >= 'A' && charcter <= 'Z')
	{
		return (1);
	}
	else

	return (0);
}
